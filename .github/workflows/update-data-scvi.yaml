name: update-data-scvi

on:
  workflow_dispatch:
  schedule:
    - cron: "30 18 * * 0"

permissions:
  contents: write
  pull-requests: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout docs branch
        uses: actions/checkout@v4
        with:
          ref: docs
          path: docs-src

      - name: Checkout package source
        uses: actions/checkout@v4
        with:
          ref: devel
          path: sclet-src

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "release"
          use-public-rspm: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libhdf5-dev \
            libpng-dev \
            libjpeg-dev \
            libtiff5-dev \
            libfreetype6-dev \
            libfontconfig1-dev

      - name: Install dependencies
        run: |
          Rscript -e 'install.packages(c("remotes", "BiocManager"))'
          Rscript -e 'BiocManager::install(c("Rhdf5lib", "rhdf5filters", "rhdf5", "h5mread", "HDF5Array", "bluster", "TENxPBMCData", "scuttle", "scran", "SingleCellExperiment", "SummarizedExperiment", "basilisk", "reticulate", "zellkonverter"), ask = FALSE, update = FALSE, dependencies = TRUE)'
          Rscript -e 'remotes::install_github(c("YuLab-SMU/ggsc", "YuLab-SMU/yulab.utils", "YuLab-SMU/fanyi"), upgrade = "never")'
          Rscript -e 'remotes::install_deps("sclet-src", dependencies = c("Depends", "Imports", "LinkingTo"), upgrade = "never")'
          R CMD INSTALL sclet-src

      - name: Build scVI cache
        run: |
          set -euo pipefail
          ws="$GITHUB_WORKSPACE"
          bookdir=$(dirname "$(find "$ws/docs-src" -maxdepth 6 -name '_bookdown.yml' -print -quit)")
          mkdir -p "$bookdir/data"
          cd "$bookdir/data-raw"
          Rscript -e 'source("pbmc-scvi.R")'
          cp -f "$bookdir/data/rm_scvi_pbmc.rds" "$bookdir/data/"

      - name: Create pull request
        uses: peter-evans/create-pull-request@v6
        with:
          path: docs-src
          branch: update-data-scvi
          base: docs
          title: "chore(data): update scVI integration cache"
          commit-message: "chore(data): update scVI integration cache"
          body: "Update cached scVI integration output generated from bookdown/data-raw/pbmc-scvi.R."
          add-paths: |
            data/rm_scvi_pbmc.rds
