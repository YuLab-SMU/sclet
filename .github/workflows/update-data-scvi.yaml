name: update-data-scvi

on:
  workflow_dispatch:
  schedule:
    - cron: "30 18 * * 0"

permissions:
  contents: write
  pull-requests: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout docs branch
        uses: actions/checkout@v4
        with:
          ref: docs
          path: docs-src

      - name: Checkout package source
        uses: actions/checkout@v4
        with:
          ref: devel
          path: sclet-src

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "release"
          use-public-rspm: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev libhdf5-dev libpng-dev libjpeg-dev libtiff5-dev libfreetype6-dev libfontconfig1-dev

      - name: Install dependencies
        run: |
          Rscript -e 'install.packages(c("remotes", "BiocManager"))'
          Rscript -e 'BiocManager::install(c("TENxPBMCData", "scuttle", "scran", "SingleCellExperiment", "SummarizedExperiment", "basilisk", "reticulate", "zellkonverter"), ask = FALSE, update = FALSE)'
          Rscript -e 'remotes::install_github(c("YuLab-SMU/ggsc", "YuLab-SMU/yulab.utils", "YuLab-SMU/fanyi"), upgrade = "never")'
          Rscript -e 'remotes::install_deps("sclet-src", dependencies = c("Depends", "Imports", "LinkingTo"), upgrade = "never")'
          R CMD INSTALL sclet-src

      - name: Build scVI cache
        working-directory: docs-src/bookdown/data-raw
        run: Rscript -e 'source("../../../../sclet-src/bookdown/data-raw/pbmc-scvi.R")'

      - name: Create pull request
        uses: peter-evans/create-pull-request@v6
        with:
          path: docs-src
          branch: update-data-scvi
          base: docs
          title: "chore(data): update scVI integration cache"
          commit-message: "chore(data): update scVI integration cache"
          body: "Update cached scVI integration output generated from bookdown/data-raw/pbmc-scvi.R."
          add-paths: |
            bookdown/data/rm_scvi_pbmc.rds
