% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{RunSpatialNiche}
\alias{RunSpatialNiche}
\title{Run spatial niche analysis}
\usage{
RunSpatialNiche(
  sce,
  features = NULL,
  method = c("SVP"),
  name = "spatial_niche",
  ...
)
}
\arguments{
\item{sce}{A SingleCellExperiment or SpatialExperiment object.}

\item{features}{Character vector. Column names in \code{colData(sce)} to run
LISA analysis on. If \code{NULL}, uses columns from the most recent
\code{RunSpatialDeconvolution()} result.}

\item{method}{Character. Backend. Currently only \code{"SVP"} is supported.}

\item{name}{Analysis record id. Defaults to \code{"spatial_niche"}.}

\item{...}{Additional arguments passed to \code{SVP::runLISA()}.}
}
\value{
Updated SingleCellExperiment with LISA hotspot categories in
\code{colData} (columns prefixed with \code{lisa_}) and a niche state record.
}
\description{
Run spatial niche detection
}
\details{
Detects local spatial hotspots (niches) for specified features using
SVP's Local Indicators of Spatial Association (LISA). For each feature,
\code{runLISA()} categorizes every spot as High-High (hotspot), Low-Low,
High-Low, Low-High, or not significant, identifying regions where a
cell state is spatially enriched above random expectation.
}
