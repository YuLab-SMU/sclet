% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priority.R
\name{RunPerturbationPriority}
\alias{RunPerturbationPriority}
\title{Run perturbation priority analysis with Augur}
\usage{
RunPerturbationPriority(
  sce,
  condition_col,
  group_col = NULL,
  layer = NULL,
  assay = NULL,
  name = "augur",
  ...
)
}
\arguments{
\item{sce}{A SingleCellExperiment object.}

\item{condition_col}{Character. Column in \code{colData(sce)} that contains
condition labels (e.g. treatment vs control).}

\item{group_col}{Character. Column in \code{colData(sce)} that contains cell
type or cluster labels. If \code{NULL}, defaults to \code{ActiveIdent(sce)}.
The special value \code{"colLabels"} uses \code{SingleCellExperiment::colLabels(sce)}.}

\item{layer}{Character. Expression layer to use for the Augur analysis.
If \code{NULL}, uses the active layer and avoids scaled layers.}

\item{assay}{Character. Assay to use if \code{layer} is not specified.}

\item{name}{Character. Analysis record id. Defaults to \code{"augur"}.}

\item{...}{Additional arguments passed to \code{Augur::calculate_auc()}.}
}
\value{
A \code{SingleCellExperiment} object with perturbation priority results
registered in the analysis-state layer. The raw Augur result list is also
stored under \code{metadata(sce)$sclet$analyses$augur} for backwards
compatibility.
}
\description{
Prioritize cell types by their responsiveness to experimental perturbation
using the Augur framework. Augur trains a machine-learning model for each
cell type to predict experimental condition from molecular measurements, then
ranks cell types by cross-validated AUC.
}
