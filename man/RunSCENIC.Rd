% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenic.R
\name{RunSCENIC}
\alias{RunSCENIC}
\title{Run pySCENIC Pipeline}
\usage{
RunSCENIC(
  sce,
  tfs_path,
  motif_annotations_path,
  database_paths,
  assay_use = "counts",
  num_workers = 1L,
  seed = 123L
)
}
\arguments{
\item{sce}{A SingleCellExperiment object.}

\item{tfs_path}{Character. Path to the transcription factors list file (e.g., a \code{.txt} file with one TF per line).}

\item{motif_annotations_path}{Character. Path to the motif annotations file (e.g., a \code{.tbl} file).}

\item{database_paths}{Character vector. Paths to the cisTarget ranking databases (\code{.feather} files).}

\item{assay_use}{Character. The name of the assay to use for inference. Defaults to "counts".}

\item{num_workers}{Integer. Number of workers for parallel processing. Defaults to 1.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to 123.}
}
\value{
A SingleCellExperiment object with the SCENIC AUC matrix stored in \code{altExp(sce, "SCENIC_AUC")}
and the state registered under \code{metadata(sce)$sclet$analyses$scenic}.
}
\description{
This function runs the pySCENIC pipeline (GRN inference, motif enrichment, and AUCell scoring)
via \code{basilisk} to isolate the Python environment. The resulting AUC matrix is stored as an
alternative experiment (\code{altExp}) in the SingleCellExperiment object, and the analysis state
is updated to track the SCENIC results.
}
