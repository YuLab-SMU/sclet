% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priority.R
\name{RunRareCellDetection}
\alias{RunRareCellDetection}
\title{Run rare cell detection}
\usage{
RunRareCellDetection(
  sce,
  method = c("density"),
  reduction = "PCA",
  dims = 1:20,
  k = 20,
  q_threshold = 0.25,
  rare_threshold = 10,
  name = "rareq"
)
}
\arguments{
\item{sce}{A SingleCellExperiment object with a reduction (e.g. PCA).}

\item{method}{Character. Currently only \code{"density"} is supported.}

\item{reduction}{Character. Reduction to use. Defaults to "PCA".}

\item{dims}{Integer vector. Dimensions to use. Defaults to 1:20.}

\item{k}{Integer. Number of nearest neighbors for the kNN graph. Defaults to 20.}

\item{q_threshold}{Numeric. Mutual connectivity threshold above which a cell
is considered a high-density seed. Range 0-1. Defaults to 0.25.}

\item{rare_threshold}{Integer. Clusters with ≤ this many cells are flagged
as rare. Defaults to 10.}

\item{name}{Analysis record id. Defaults to \code{"rareq"}.}
}
\value{
Updated SingleCellExperiment with \code{rare_cluster} in colData.
}
\description{
Identifies rare and major cell populations using local density analysis
on the kNN graph. The algorithm computes a mutual connectivity metric
(Q-value) from each cell's k-nearest neighbors, identifies locally dense
micro-clusters as seeds, propagates labels outward, and flags small
clusters as rare populations.
}
