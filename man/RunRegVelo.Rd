% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regvelo.R
\name{RunRegVelo}
\alias{RunRegVelo}
\title{Run RegVelo RNA velocity analysis}
\usage{
RunRegVelo(
  sce,
  grn,
  regulators = NULL,
  spliced_assay = "spliced",
  unspliced_assay = "unspliced",
  reduction = NULL,
  name = "regvelo",
  max_epochs = 1500L,
  lam = 1,
  lam2 = 0,
  batch_size = NULL,
  seed = 0L,
  backend = c("basilisk", "reticulate"),
  python = NULL,
  ...
)
}
\arguments{
\item{sce}{A SingleCellExperiment object.}

\item{grn}{Prior gene regulatory network. Either a numeric matrix with
targets in rows and regulators in columns, or a data.frame with
\code{target}, \code{regulator}, and optional \code{weight} columns.}

\item{regulators}{Optional character vector of regulator genes. If \code{NULL},
regulators are inferred from \code{grn}.}

\item{spliced_assay, unspliced_assay}{Assays containing spliced and unspliced
counts.}

\item{reduction}{Optional dimensionality reduction copied to AnnData obsm.
Defaults to \code{DefaultReduction(sce)}.}

\item{name}{Velocity record id. Defaults to \code{"regvelo"}.}

\item{max_epochs}{Maximum training epochs passed to RegVelo.}

\item{lam, lam2}{RegVelo regularization parameters.}

\item{batch_size}{Optional training batch size.}

\item{seed}{Optional random seed.}

\item{backend}{Python execution backend. \code{"basilisk"} creates the packaged
CPU environment; \code{"reticulate"} uses the caller-configured Python, which is
preferred on GPU clusters.}

\item{python}{Optional Python executable or conda environment name used when
\code{backend = "reticulate"}.}

\item{...}{Additional parameters passed to the RegVelo model constructor.}
}
\value{
A SingleCellExperiment object with RegVelo velocity state updated.
}
\description{
Run RegVelo through either an isolated \code{basilisk} Python environment or the
active \code{reticulate} Python environment and register its outputs as a sclet
velocity state. RegVelo combines spliced/unspliced counts with a prior gene
regulatory network.
}
