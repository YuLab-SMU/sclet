% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{RunTrajectoryWorkflow}
\alias{RunTrajectoryWorkflow}
\title{Run a trajectory-oriented analysis workflow}
\usage{
RunTrajectoryWorkflow(
  sce,
  group = NULL,
  reduction = NULL,
  run_diffusion_map = TRUE,
  run_slingshot = TRUE,
  run_velocity = c("auto", "never", "always"),
  run_fate = c("auto", "never", "always"),
  diffusion_components = 50,
  velocity_mode = c("deterministic", "stochastic", "dynamical"),
  start_cluster = NULL,
  end_cluster = NULL,
  name = "trajectory_workflow"
)
}
\arguments{
\item{sce}{A SingleCellExperiment object.}

\item{group}{Grouping variable used for trajectory inference. If \code{NULL}, uses
\code{ActiveIdent(sce)}. The special value \code{"colLabels"} is materialized to a
stable \code{colData} column for downstream methods that require a column name.}

\item{reduction}{Reduction used by Slingshot/velocity/fate. If \code{NULL}, uses
\code{DefaultReduction(sce)} and falls back to \code{"PCA"} when available.}

\item{run_diffusion_map}{Logical. Whether to compute \code{RunDiffusionMap()}.}

\item{run_slingshot}{Logical. Whether to run \code{RunSlingshot()}.}

\item{run_velocity}{One of \code{"auto"}, \code{"never"}, or \code{"always"}.}

\item{run_fate}{One of \code{"auto"}, \code{"never"}, or \code{"always"}.}

\item{diffusion_components}{Number of diffusion components.}

\item{velocity_mode}{Velocity mode passed to \code{RunVelocity()}.}

\item{start_cluster, end_cluster}{Optional lineage root/end clusters for Slingshot.}

\item{name}{Workflow analysis id. Defaults to \code{"trajectory_workflow"}.}
}
\value{
Updated SingleCellExperiment object.
}
\description{
This high-level entry point organizes the trajectory mainline around the
user's analysis goal instead of exposing only individual algorithms. It can
orchestrate diffusion map, Slingshot, RNA velocity, and CellRank/CellFate
using the existing atomic interfaces in \code{sclet}.
}
